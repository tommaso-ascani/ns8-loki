{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "$id": "http://schema.nethserver.org/loki/get-forwarder.json",
    "title": "Configuration of Loki 2 Syslog",
    "description": "Retrieve the configuration of loki2syslog instance.",
    "type": "object",
    "required": [
        "syslog_ip",
        "syslog_port",
        "syslog_protocol",
        "syslog_format"
    ],
    "properties": {
        "syslog_ip": {
            "type": "string",
            "description": "Syslog server ip address."
        },
        "syslog_port": {
            "type": "integer",
            "description": "Syslog server port."
        },
        "syslog_protocol": {
            "type": "string",
            "description": "Trasmission protocol."
        },
        "syslog_format": {
            "type": "string",
            "description": "Logs RFC format."
        }
    }
}
